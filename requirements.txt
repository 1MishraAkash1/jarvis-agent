# Phase 1 — core agent loop (local model via Ollama)
# Requires the Ollama app/daemon installed and running separately —
# this package is just the Python client that talks to it.
ollama>=0.4.0

# Phase 2 — RAG (add when we get there)
# chromadb>=0.5.0
# sentence-transformers>=3.0.0

# Phase 4 — Telegram interface
python-telegram-bot>=21.0
python-dotenv>=1.0.0

# Phase 5a — voice IN (speech-to-text)
# CTranslate2-backed Whisper, not openai-whisper — avoids pulling in PyTorch
# (~2GB) for something this does without it. First transcription auto-downloads
# the "base" model (~150MB) from Hugging Face, then it's cached locally.
faster-whisper>=1.0.0

# Phase 5b — voice OUT (text-to-speech)
# Piper runs on ONNX Runtime, not PyTorch — same "avoid heavy deps" reasoning
# as everything else in this project. Unlike faster-whisper, the voice model
# needs a one-time manual download (see README) rather than auto-downloading.
piper-tts>=1.8.0